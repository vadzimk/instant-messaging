# workflow:
#   rules:
#     - if: $CI_COMMIT_BRANCH != "master" && $CI_PIPELINE_SOURCE != "merge_request_event"
#       when: never
#     - when: always


stages:
  - build

init-test-postgres-job:
  stage: build
  before_script:
    - set -o allexport &&
      source $ENV_TEST &&
      set +o allexport
  script:
    - echo $AUTH_ON

